namespace TienLen.Presentation.Shared
{
    /// <summary>
    /// Defines how a message should be displayed to the player.
    /// </summary>
    public enum MessageDisplayMode
    {
        Toast = 0,
        Modal = 1,
        Fullscreen = 2
    }
}
